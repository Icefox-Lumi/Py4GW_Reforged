# Items Documentation

This topic owns item behavior, modifier decoding, catalogs, and their
supporting extraction tooling. The owning Python/native implementation and
live-client observation remain authoritative.

- `modifiers/` contains the item-modifier record set.
- `modifiers/generated/` contains generated catalog and capture output.
- `modifiers/tools/` contains the scripts that produce or inspect that output.
