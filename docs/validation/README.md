# Validation Documentation

This topic owns cross-layer validation records and reproducible runtime
evidence. It does not replace current source or live injected-client proof.

- `demo/` records the demo replacement's coverage, API surface, and gaps.
- `demo/research/` preserves the supporting wrapper and binding investigation.
