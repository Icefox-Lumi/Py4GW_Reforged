# Hex-Removal Automation Documentation

This directory contains the hex-removal priority, configuration, selection,
coordination, GUI, and runtime-authoring record.

- `hex_removal_architecture_and_authoring.md` is an implementation/design
  record. Verify its paths, defaults, and migration claims against current
  `BuildMgr`, settings, skills, and HeroAI sources before changing behavior.
- It is separate from generic behavior-tree and HeroAI subsystem records, even
  where those systems are used by the hex-removal flow.
