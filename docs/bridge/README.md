# Integration Documentation

Use this directory for boundaries between the injected client, shared memory,
bridge daemon, MCP adapter, and external operators.

- `mcp/` — bridge/MCP planning and operator/runtime references.
- `shared-memory/` — shared-memory transport plans, postmortems, and ownership.

Keep transport contracts distinct from gameplay and UI implementation records.
